services:
  - type: web
    name: slack-webhook-proxy
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: TASKLET_WEBHOOK_URL
        sync: false
      - key: PYTHON_VERSION
        value: 3.11.0
